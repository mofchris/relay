# Copy to .env and adjust as needed (the server runs fine with defaults).
PORT=8787
CORS_ORIGIN=http://localhost:5173
# Used to sign auth tokens. Change this for any non-local deployment.
JWT_SECRET=relay-dev-secret-change-me
# Optional: override where the SQLite file lives.
# DB_PATH=./relay.db
